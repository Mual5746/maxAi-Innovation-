import './Title.css'
// eslint-disable-next-line react/prop-types
const Title = ({ subTitle }) => {
  return (
    <div className='title'>
      <p>{subTitle}</p>

    </div>
  )
}

export default Title
